# Local: Vite proxies /api to the Worker (wrangler dev on :8787)
# Production build: leave empty so the SPA calls the same origin.
VITE_API_BASE_URL=http://localhost:8787
VITE_WS_URL=ws://localhost:8787
